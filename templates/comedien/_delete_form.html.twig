<form method="post" action="{{ path('app_comedien_delete', {'id': comedien.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comedien.id) }}">
    <button class="btn">Delete</button>
</form>
