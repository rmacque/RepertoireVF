{{ form_start(form) }}
    {#{ form_widget(form) }#}
    {{ form_row(form.nom) }}
    {{ form_row(form.prenom) }}
    {#% if is_granted('ROLE_ADMIN') %}
      {% do form_row(form.DA).setRendered() %}
    {% endif %#}
    {{ form_row(form.DA) }}
    {{ form_row(form.photo) }}
    <button class="btn">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
